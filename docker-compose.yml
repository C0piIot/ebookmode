services:
  ebookmode:
    container_name: ebookmode
    ports:
      - "8080:8080"
    build:
      context: .
      target: development
    volumes:
      - .:/app
      - gomodcache:/root/go/pkg/mod
volumes:
  gomodcache:
